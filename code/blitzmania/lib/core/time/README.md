# Time Module

A pair of files which allow a server and clients to synchronise their game clocks. Implementation is hidden from the Core, but not the programmer. This means that if an implementation changes, the Core module stays the same, but the programmer may need to change surrounding code.
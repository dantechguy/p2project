import 'package:blitz/src/core/event_serialiser/str_event_conversion.dart' show convertEventToJsonStringForServer, convertJsonStringFromServerToEvent;
import 'package:test/test.dart';

void main() {
  // TODO: Write test after figured out [Event] type and instantiation restrictions etc.
  // test('Generated event IDs should be unique.', () {
  //   final idGenerator = EventIDGenerator();
  //   final generatedIDs = <dynamic>{};
  //
  //   // Generate 1000 IDs and check all unique.
  //   for (int i = 0; i < 1000; i++) {
  //     final id = idGenerator.generateUniqueID();
  //     expect(generatedIDs.contains(id), false);
  //     generatedIDs.add(id);
  //   }
  // });
}
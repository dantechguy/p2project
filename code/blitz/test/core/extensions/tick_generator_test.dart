import 'package:blitz/src/core/extensions/tick_generator.dart' show addTickEvents;
import 'package:test/test.dart';

void main() {
  // TODO: This module isn't functional yet, as the StreamController implementation is leaky.
  // TODO: Even so, we'd need to fake the timer callbacks and manually call them.
  // TODO: Then we could add a series of events and timer callbacks, convert the stream output to a list, and check it.
  // test('', () {
  //   expect();
  // });
}
